<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korit.springboot_study.mapper.PostLikeMapper">

    <resultMap id="likeCountResultMap" type="com.korit.springboot_study.entity.PostLike">
        <result property="postId" column="post_id" />
        <result property="likeCount" column="like_count" />
    </resultMap>

    <insert id="insert" keyProperty="postLikeId" useGeneratedKeys="true">
        insert into
            post_like_tb
        values
            (default, #{postId}, #{userId}, now())
    </insert>

    <delete id="deleteByPostIdAndUserId">
        delete
        from
            post_like_tb
        where
            post_id = #{post_id}
        and
            user_id = #{user_id}
    </delete>

    <select id="selectLikeCountByPostId" resultType="java.lang.Integer">
        select
            post_id,
            count(post_id) as like_count
        from
            post_like_tb
        where
            post_id = #{postId}
        group by
            post_id
    </select>

    <select id="selectLikeCountAll" resultMap="likeCountResultMap">
        select
            post_id,
            count(post_id) as like_count
        from
            post_like_tb
        group by
            post_id
    </select>
</mapper>
